apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: {{ .Values.environment.namespace | quote }}
  labels:
    app.kubernetes.io/name: finopsiq
    app.kubernetes.io/component: notification-service
spec:
  replicas: {{ .Values.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/name: finopsiq
      app.kubernetes.io/component: notification-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: finopsiq
        app.kubernetes.io/component: notification-service
        azure.workload.identity/use: "true"
    spec:
      serviceAccountName: notification-service
      nodeSelector:
{{ toYaml .Values.nodeSelector | indent 8 }}
      tolerations:
{{ toYaml .Values.tolerations | indent 8 }}
      {{- if and .Values.config.csi.enabled .Values.config.keyVaultName .Values.services.notification.serviceAccountClientId }}
      volumes:
        - name: app-config-secrets
          csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: {{ printf "%s-notification-service" .Values.config.secretProviderClassPrefix | quote }}
      {{- end }}
      containers:
        - name: notification-service
          image: "{{ required "services.notification.image.repository is required" .Values.services.notification.image.repository }}:{{ .Values.services.notification.image.tag }}"
          imagePullPolicy: {{ .Values.imagePullPolicy }}
          {{- if and .Values.config.csi.enabled .Values.config.keyVaultName .Values.services.notification.serviceAccountClientId }}
          volumeMounts:
            - name: app-config-secrets
              mountPath: /mnt/secrets-store
              readOnly: true
          {{- end }}
          ports:
            - name: http
              containerPort: {{ .Values.services.notification.port }}
          envFrom:
            - secretRef:
                name: {{ .Values.config.secretName | quote }}
          env:
{{ include "finopsiq.commonEnv" . | indent 12 }}
{{ include "finopsiq.workloadIdentityEnv" (dict "clientId" .Values.services.notification.serviceAccountClientId) | indent 12 }}
            - name: SERVICE_MODULE
              value: {{ .Values.services.notification.serviceModule | quote }}
          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
            initialDelaySeconds: 10
            periodSeconds: 15
            timeoutSeconds: 5
          livenessProbe:
            httpGet:
              path: /health/live
              port: http
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 5
          resources:
            requests:
              cpu: {{ .Values.resources.requests.cpu | quote }}
              memory: {{ .Values.resources.requests.memory | quote }}
            limits:
              cpu: {{ .Values.resources.limits.cpu | quote }}
              memory: {{ .Values.resources.limits.memory | quote }}
